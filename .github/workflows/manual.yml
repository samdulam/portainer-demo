name: "Cluster Deploy"
on:
  workflow_dispatch:
    inputs:
      Name:
        description: 'Enter a Name for Cluster:'
        type: string
        required: true
      Dataaacenter:
        description: 'DataCenter to deploy the cluster in:'
        type: string
        required: true
      Num of Control Plane Nodes:
        description: "Control plane nodes needed:"
        required: false
        type: string
env:
  SECRET1: ${{ secrets.SECRET1 }}
  VAR1: ${{ vars.VAR1 }}
  ClusterName: ${{ inputs.Name }}
jobs:
  test:
    runs-on: sdulam
    steps:
      - name: "First Step"
        run: |
          pwd
          env
          echo "Secret: "$SECRET1
          echo "Var: "$VAR1
          echo "ClusterName: ${{ inputs.Name }}"
          
